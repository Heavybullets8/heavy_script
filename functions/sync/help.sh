#!/bin/bash


sync_help(){
    echo -e "${bold}Sync${reset}"
    echo -e "${bold}----${reset}"
    echo -e "${blue}heavyscript sync${reset} | [--help]"
    echo
    echo -e "${bold}Description${reset}"
    echo -e "${bold}-----------${reset}"
    echo -e "Sync the catalog"
    echo
    echo -e "${bold}Options${reset}"
    echo -e "${bold}-------${reset}"
    echo -e "${blue}-h${reset} | ${blue}--help${reset}"
    echo -e "    Display this help message."
    echo
}